// SOW-029: single source for the GBTI Discord invite link, shared by the welcome view (and future surfaces).
// PLACEHOLDER until provisioning. HUMAN-TODO: set the real community invite, then rebuild client-ui + the
// extension (node client-ui/build.mjs && npm run build:extension) and commit the artifacts.
export const DISCORD_INVITE_URL = 'https://discord.gg/gbti-network';
