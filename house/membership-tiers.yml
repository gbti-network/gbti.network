# sow-185: the canonical membership tier data. SINGLE SOURCE OF TRUTH for both the homepage pricing accordion +
# the Your-membership/Upgrade rail cards (sow-192, rendered by the presentation layer) AND the tier gating
# (sow-185). The tier IDENTITY + ranking + price-id->tier resolution live in membership/tiers.mjs (the axis);
# the actual Stripe price ids live in the Worker env (owner-provisioned). This file carries DISPLAY data (label,
# tagline, prices, benefit bullets, revenue copy) + the price-id ENV VAR NAMES for the checkout allowlist.
#
# The build reads this directly via src/lib/tiers.ts (no JSON artifact), scripts/validate-content.mjs validates
# its shape, and membership/tiers-display.mjs is the shared pure parser. Superadmin-CODEOWNED, reviewed in a PR.
#
# BENEFIT COPY IS A LEGAL LINE ON A PAID PAGE. Per sow-185 section 7 (the offerings register): a tier may ship
# with unbuilt benefits removed, but it must NEVER advertise a benefit that does not exist. Every bullet below is
# an "already built" offering (section 7) or a settled SOFTEN wording. Deliberately EXCLUDED until they ship:
# instant notifications (sow-150), advanced custom feeds, member-only curation pools (DROP), the weekly digest
# (sow-166), a free-tier save limit (DROP), creator analytics, AI editorial review (softened to human), the
# founding badge, and "sell products / sponsorships" (mockup-only, not built). The Creator "connect a news
# source" privilege is held until its governed workflow (sow-185 phase 2b) ships. "by application" on Creator is
# mockup-only and NOT in sow-185; it is an OPEN owner question and is intentionally not encoded here.
#
# REMOVED 2026-08-26, applying the owner ruling of 2026-08-18 (sow-201, Option A: FIX THE COPY, DO NOT GATE):
#   - Member "Curate your feed" -> "See the member-only Shares stream". Feed curation is a FREE-tier capability
#     (the free tier advertises the personalized feed and the new-tab extension, and the endpoints behind it
#     admit any signed-in account), so selling it at $5 breached this header's own rule. Making the copy true
#     by ENFORCEMENT was on the table and was DECLINED: clawing back something free accounts already have, in
#     order to justify a sales line, is the worse of the two repairs. The replacement is genuinely gated
#     (workers/signup/membership-shares.mjs: READ_MEMBERS is paid + trialing, so a free account is filtered to
#     public shares). Recorded honestly: that is 7 members-only shares out of 50 today, a real benefit and a
#     thin headline, and the owner chose it knowing the number.
#   - Creator "Weekly product-development shop talk" -> removed entirely. It is POSITIONING, not software, and
#     section 7 requires a named owner and a cadence before such a thing appears in PRICING copy. It already
#     lives in general community copy (src/components/JoinOffers.astro, "Weekly Shop Talk calls"), so nothing
#     was written to replace it and no new commitment was invented.
#     CORRECTION, same day: JoinOffers.astro is UNMOUNTED. It has zero imports and renders on no page (its
#     own header even says "the /membership/ page is the natural future home"), so the sentence above is true
#     of the repository and false of the live site. The practical effect is that shop talk is now advertised
#     NOWHERE, which is the safe end of the ruling rather than the intended one: the owner said it moves INTO
#     general community copy. Mounting that component is a visible page change and is the owner's call, so it
#     is raised in human-todo rather than done here. Do not read this note as "it is covered elsewhere".
tiers:
  - key: none
    label: Free
    tagline: Browse, follow, and save across the network.
    priceMonthly: 0
    priceAnnual: 0
    priceEnv: {}
    benefits:
      - Browse the network, the news feed, and public shares
      - Follow creators, sources, and topics
      - Save items and build collections
      - A personalized feed and the new-tab extension
    revenue: ""

  - key: member
    label: Network Supporter
    tagline: Join the community, publish your work, and share in the revenue.
    priceMonthly: 5
    priceAnnual: 50
    priceEnv:
      monthly: STRIPE_PRICE_MEMBER_MONTHLY
      annual: STRIPE_PRICE_MEMBER_ANNUAL
    # sow-230: these four carry a `description` as well as a `label`. The label is the one-line bullet every
    # other surface renders; the description is the supporting line the invite lander shows under it. Both are
    # benefit copy and both are governed by this file's header rule, which is exactly why the description
    # lives here rather than in the page that displays it: a member invite went live advertising Creator
    # benefits because its prose was written into the template, and writing these lines into a template would
    # have reopened that hole one field over. A bare string is still valid for every other entry.
    benefits:
      - label: Post comments across the network
        description: Reply anywhere on the network under your own handle, not a walled-off forum.
      - label: Full Discord access
        description: The members' Discord, where the day-to-day conversation and help actually happens.
      - label: Read members-only content and comment bodies
        description: Read the posts and comment bodies that are hidden from logged-out visitors.
      - label: See the member-only Shares stream
        description: Read the members-only side of the Shares stream, not just the public links every visitor sees.
      - label: Publish articles, projects and prompts, members first and public after editorial review
        description: Write in the WorkBench and publish to the network. Work goes out to members first; a superadmin reviews it before it appears on the public site.
    revenue: Supporters share in the [revenue program](/revenue-model/).

  # sow-323: NOT OFFERED since 2026-09-12. The owner collapsed the two paid plans into one, so nothing sells or
  # applies for this tier any more. The entry STAYS, and deleting it would break the build in three places
  # (npm test, npm run check:content, then astro build: parseTierDisplay requires every axis tier), but that is
  # not the only reason to keep it. The tier is still real internally: a superadmin grants it silently to a
  # member who no longer needs editorial review, staff resolve to it through their role, and the legacy Stripe
  # price maps to it. Its label is what those holders carry, so it stays accurate even though no card shows it.
  # The benefits below are frozen as the historical record of what the plan offered; they are not advertised.
  - key: creator
    label: Curator
    offered: false
    tagline: Publish, get discovered, and share in the revenue.
    priceMonthly: 15
    priceAnnual: 150
    priceEnv:
      monthly: STRIPE_PRICE_CREATOR_MONTHLY
      annual: STRIPE_PRICE_CREATOR_ANNUAL
    benefits:
      - Publish articles, products, and prompts
      - Post to the member Shares stream
      - Your creator profile and unified portfolio
      - Reshare and syndicate your work
    revenue: Curators participate in the greater [revenue program](/revenue-model/).
