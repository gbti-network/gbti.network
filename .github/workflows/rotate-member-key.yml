name: Rotate member-content key

# SOW-016 key-destroying rotation. Re-encrypts every published .enc member-content asset under a NEW epoch key,
# then opens a PR (the .enc files + the bumped MEMBER_CONTENT_KID in wrangler.toml) for human review. After the
# PR merges and the CDN propagates, the operator swaps the Worker secret MEMBER_CONTENT_KEY to the new key and
# DESTROYS the old one (do not keep it in MEMBER_CONTENT_KEYS beyond a brief overlap), so the old git-history
# ciphertext becomes keyless. Honest limit: rotation protects only FUTURE content + bounds a key compromise to
# the window; it cannot retract plaintext already saved by a member.
#
# Manual-only by design: rotating a security key is human-gated (a botched swap locks every member out). The
# NEW key is supplied as a pre-generated secret (MEMBER_CONTENT_KEY_NEW), never minted/echoed in CI logs.
# A monthly cron is provided but commented; enable it only once the manual flow is proven.

on:
  workflow_dispatch:
    inputs:
      new_kid:
        description: 'The new epoch id (bump from the current MEMBER_CONTENT_KID, e.g. 2)'
        required: true
        type: string
  # schedule:
  #   - cron: '0 6 1 * *'   # 06:00 UTC on the 1st of each month (enable after the manual flow is proven)

permissions:
  contents: read

concurrency:
  group: rotate-member-key
  cancel-in-progress: false

jobs:
  rotate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci

      # sow-313 (found while hardening the purge workflow next door): VALIDATE THE INPUT BEFORE USING IT.
      #
      # `new_kid` is a free-text workflow_dispatch input and it was interpolated straight into six shell
      # commands below, including `git commit -m` and `gh pr create --body`. `${{ inputs.x }}` written inline
      # is textual substitution into the shell SOURCE before bash parses it, so a value carrying a quote or
      # $(...) executes. This job holds MEMBER_CONTENT_KEY, MEMBER_CONTENT_KEY_NEW and GH_BOT_TOKEN, which is
      # the highest-value secret set in the repository, so the blast radius is the whole encryption scheme
      # even though dispatching needs write access.
      #
      # Two layers, and this is the first: an epoch id is a NUMBER (`MEMBER_CONTENT_KID = "1"` in
      # wrangler.toml). Constraining it to digits removes the vector rather than escaping it. The steps below
      # then take it through `env` as the second layer, so neither one is load-bearing alone.
      - name: Validate the epoch id
        env:
          NEW_KID: ${{ inputs.new_kid }}
        run: |
          case "$NEW_KID" in
            ''|*[!0-9]*)
              echo "::error::new_kid must be digits only (the epoch id, e.g. 2). Got: $NEW_KID"
              exit 1
              ;;
          esac
          echo "epoch id $NEW_KID accepted"

      # Re-encrypt all .enc under the NEW key. Two-phase + atomic: if any asset fails to re-encrypt (a wrong
      # old key), nothing is written and the job fails. --no-print-key keeps the key out of the logs.
      - name: Re-encrypt member content to the new epoch
        run: node scripts/rotate-member-key.mjs --new-kid "$NEW_KID" --apply --no-print-key
        env:
          NEW_KID: ${{ inputs.new_kid }}
          MEMBER_CONTENT_KEY: ${{ secrets.MEMBER_CONTENT_KEY }}          # the CURRENT (old) epoch key
          MEMBER_CONTENT_KEY_NEW: ${{ secrets.MEMBER_CONTENT_KEY_NEW }}  # the NEW epoch key (pre-generated)

      # MEMBER_CONTENT_KID appears TWICE in wrangler.toml: under [vars] and under [env.production.vars].
      # Production runs the [env.production] environment (the deploy is --env production), so the second one
      # is the live value. A NON-global replace rewrites only the FIRST, inert, occurrence and leaves
      # production on the old epoch; after the secret swap every member-only decrypt then fails in production.
      # Hence /gm, plus an assertion that it actually matched: a silent no-op on a regex miss is how this
      # class of defect hides, because the step stays green and the damage only appears after the
      # irreversible key destruction. Fixed 2026-08-11 (SecurityMaster); see
      # .data/ops/encryption-ops/rotation-runbook.md.
      - name: Bump MEMBER_CONTENT_KID in wrangler.toml
        env:
          NEW_KID: ${{ inputs.new_kid }}
        run: |
          node -e '
            const fs = require("fs");
            const p = "workers/signup/wrangler.toml";
            const kid = process.argv[1];
            const before = fs.readFileSync(p, "utf8");
            const after = before.replace(/^MEMBER_CONTENT_KID = ".*"$/gm, `MEMBER_CONTENT_KID = "${kid}"`);
            if (after === before) {
              console.error("rotate: MEMBER_CONTENT_KID not found in " + p + "; refusing to continue.");
              process.exit(1);
            }
            const n = (after.match(/^MEMBER_CONTENT_KID = /gm) || []).length;
            console.log("rotate: set MEMBER_CONTENT_KID to " + kid + " in " + n + " location(s)");
            fs.writeFileSync(p, after);
          ' "$NEW_KID"

      # Open a PR for review (NOT auto-merge): a human verifies the rotation before the Worker secret is swapped.
      # gbtilabs is a superadmin, so the gate accepts this PR even though it touches Tier S wrangler.toml.
      - name: Open the rotation PR
        env:
          GH_TOKEN: ${{ secrets.GH_BOT_TOKEN }}
          NEW_KID: ${{ inputs.new_kid }}
        run: |
          set -e
          BRANCH="rotate-member-key-$NEW_KID"
          git config user.name "gbtilabs"
          git config user.email "gbtilabs@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add -A
          git commit -m "SOW-016: rotate member-content to epoch $NEW_KID"
          git push -u origin "$BRANCH"
          gh pr create --fill --title "Rotate member-content key to epoch $NEW_KID" \
            --body "Automated SOW-016 re-encryption to epoch $NEW_KID. After merge + CDN propagation, set the Worker secret MEMBER_CONTENT_KEY to the new key and destroy the old one (remove it from MEMBER_CONTENT_KEYS after the overlap). See .data/ops/member-ops/runbook-client-install.md."
